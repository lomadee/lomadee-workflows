# lomadee-workflows

Reusable GitHub Actions workflows for Lomadee infrastructure (Coolify + DOCR).

## coolify-build-deploy.yml

Build Docker image on GHA, push to DOCR, trigger Coolify deploy (pull-only on prod host).
